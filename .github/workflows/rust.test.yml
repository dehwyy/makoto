name: Rust tests

on:
  push:
    branches:
      - v3

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: run tests
        run: cargo test --verbose
